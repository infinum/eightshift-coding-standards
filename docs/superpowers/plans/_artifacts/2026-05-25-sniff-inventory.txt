# Sniff modernization inventory
# Generated: 2026-05-25 for plan docs/superpowers/plans/2026-05-25-modernize-sniffs-to-php-8.md

Scope: Eightshift/Sniffs/**/*.php
       Eightshift/Helpers/*.php  (directory does not exist — no helper sources)

Discovered files (3):

1. Eightshift/Sniffs/Commenting/FunctionCommentSniff.php
   Status: NO MEANINGFUL CANDIDATES — skip commit.
   Reasoning:
   - Property `$allowedExtendedClasses` is already a typed `array`.
   - `process(File $phpcsFile, $stackPtr)` overrides PHPCS Sniff interface; adding
     a param type narrows the contract (LSP violation), and adding a return type
     is unsafe because `parent::process()` returns `mixed` per the interface.
   - Body is already early-return, strict `in_array`, short array syntax.
   - No `strpos`, no `switch`, no untyped props remain.

2. Eightshift/Sniffs/Security/HelpersEscapeSniff.php
   Status: MODERNIZE.
   Candidates:
   - Replace `\strpos($haystack, $needle) !== false` with `\str_contains($haystack, $needle)`.
     Occurrences (9): lines 109, 148, 149, 212, 213, 266, 267, 274, 275.
   - All properties already typed; constructor not present (defaults inline) so
     constructor promotion / readonly do not apply.
   - `process_token($stackPtr)`, `checkIfImportInformation()`, `getEchoToken()`
     signatures left as-is: parent `EscapeOutputSniff::process_token()` and the
     PHPCS Sniff interface contract block stricter param/return types.
   - The nested if/else chain (line 142+) is intentional dispatch logic; not
     converting to `match` because each branch produces side effects + early
     returns rather than a single value.

3. Eightshift/Sniffs/Shortcodes/DisallowDoShortcodeSniff.php
   Status: NO MEANINGFUL CANDIDATES — skip commit.
   Reasoning:
   - `register()` overrides PHPCS Sniff interface (no return type declared).
     Adding `: array` would be a drive-by; sniff body is already minimal.
   - `process_token($stackPtr)` overrides WordPressCS `Sniff::process_token()`;
     same LSP / mixed-return concerns as files 1 and 2.
   - No `strpos`, `switch`, untyped props, or stale idioms in the body.

Net result: one modernization commit (file 2). Files 1 and 3 are already
PHP 8-idiomatic per the plan's "smallest idiom that helps" guideline — no
drive-by rewrites.
